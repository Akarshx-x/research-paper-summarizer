# Copy this file to secrets.toml and fill in your own key.
#   cp .streamlit/secrets.toml.example .streamlit/secrets.toml
#
# secrets.toml is gitignored — it will never be committed.
# The app runs fully offline (extractive-only, no chat answers) without this key.

GEMINI_API_KEY = "AIza..."
