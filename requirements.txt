# CPU-only PyTorch wheel — without this, `pip install torch` on Streamlit
# Cloud's Linux host pulls the CUDA-bundled build (multi-GB) instead of the
# ~200MB CPU build, which will blow the platform's build size/memory limits.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.58.0
torch==2.12.1
transformers==5.12.1
numpy==2.5.0
pandas==3.0.3
requests==2.34.2
pdfplumber==0.11.10
nltk==3.9.4
scikit-learn==1.9.0
rouge_score==0.1.2
